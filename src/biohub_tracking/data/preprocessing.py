"""
Utilities for preprocessing raw cell tracking data formats.
"""

from pathlib import Path

def preprocess_zarr_to_tiff(zarr_path: Path, output_dir: Path):
    """Converts a Zarr array to a sequence of TIFF files."""
    pass

def normalize_intensity(image_data):
    """Normalizes the intensity values of the image."""
    pass
