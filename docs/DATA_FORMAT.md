# Data Format Specification

## Input Format
The expected input is a `.zarr` file containing time-series image data.

## Output Format
The pipeline outputs a CSV file with tracking and division annotations.
