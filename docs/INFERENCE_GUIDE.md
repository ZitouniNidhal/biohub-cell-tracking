# Inference Guide

## Prerequisites
- Download the trained model weights.
- Prepare your `.zarr` data.

## Running Inference
You can run the full pipeline using the `submission.py` script:
```bash
python submission.py --test-dir /path/to/data --output results.csv
```
