# Training Guide

## Dataset Preparation
Run `scripts/download_data.sh` to download the raw dataset, and then run `scripts/preprocess_data.py`.

## Model Training
- To train segmentation: `bash scripts/train_segmentation.sh`
- To train tracking: `bash scripts/train_tracking.sh`
